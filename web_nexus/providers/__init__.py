"""
Nexus AI providers package.

Each module owns one provider or one capability. main.py orchestrates;
this package does the actual model calls. Adding a new provider = new module
+ one entry in registry.TOOLS or main.MODELS.
"""
